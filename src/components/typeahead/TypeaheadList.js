import styled from 'styled-components';

const TypeaheadList = styled.div`
  background-color: white;
  box-shadow: 5px 3px 10px rgba(0, 0, 0, 0.5);
  position: absolute;
  z-index: 10;
`;

export default TypeaheadList;
